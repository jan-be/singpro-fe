# Fallback only — in production nginx serves the robots.txt generated by the
# backend (see singpro-be/src/generate-seo.js). Keep the two in sync.
User-agent: *
Allow: /
Disallow: /join/
Disallow: /mic/
Disallow: /sing/none

Sitemap: https://singpro.app/sitemap-index.xml
